#include <bits/stdc++.h>  // Includes all standard C++ libraries
using namespace std;     // Allows use of standard names without std::

int main() {
    cout << "Hello";     // Prints "Hello" on the screen
    return 0;            // Ends program successfully
}
